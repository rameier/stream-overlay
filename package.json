{{

  "name": "stream-overlay",  "name": "stream-overlay",

  "version": "1.0.0",  "version": "1.0.0",

  "description": "Stream overlay with dashboard for OBS Browser Source",  "description": "Stream overlay with dashboard for OBS Browser Source",

  "scripts": {  "scripts": {

    "start": "cd server && npm start",    "start": "cd server && npm start",

    "dev": "cd server && npm run dev",    "dev": "cd server && npm run dev",

    "build": "cd app && npm run build",    "build": "cd app && npm run build",

    "install-server": "cd server && npm install",    "install-server": "cd server && npm install",

    "install-app": "cd app && npm install",    "install-app": "cd app && npm install",

    "install-all": "npm run install-server && npm run install-app",    "install-all": "npm run install-server && npm run install-app",

    "setup": "npm run install-all && npm run build"    "setup": "npm run install-all && npm run build"

  },  },

  "devDependencies": {}  "devDependencies": {}

}}-overlay-server",
  "version": "1.0.0",
  "description": "Express server for stream overlay with OBS Browser Source support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd app && npm run build",
    "install-all": "npm install && cd app && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
